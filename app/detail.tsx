import { Text, View } from 'react-native';

export default function Detail() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff', fontSize: 18 }}>Detail page</Text>
    </View>
  );
}
